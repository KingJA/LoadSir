
Feature
* 重新加载点击事件
* 可配置布局及状态码
* 可转换输入信号和输出状态
* 设置单独按钮事件
* 可子线程直接操作
* 可设置初始状态页面(如，加载页)

优势
* 不需要设置枚举或者常量状态值
* 可扩展状态页面
* 可对单个状态页单独设置点击事件
* 支持Activity，Fragment,View状态回调
* 支持回调实体和状态页字节码文件的转换


测试
状态保存，如Fragment的切换
多个Fragment之间的冲突
Fragment viewpager的正常显示，还需调试

默认状态
错误
无数据
加载中
有数据